package com.example.finances.core.ui.theme

import androidx.compose.runtime.staticCompositionLocalOf

val LocalThemeController = staticCompositionLocalOf<ThemeController> { error("No ThemeController") }
